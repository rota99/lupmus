/*
const idDiscussione = '774710293363949618';

module.exports = {
  name: 's',
  description: 'Comando di prova per audio.',

  async execute(client, message, args) {
    const audio = async (message) => {
      members = client.guilds.cache.get('774369837727350844').channels.cache.get(idDiscussione).members;
      const canale = message.members

      if(!canale) {
        message.reply("Vedi di essere connesso al canale vocale, altrimenti...")
        return;
      }
      canale.join()
    }
  }
}
*/
